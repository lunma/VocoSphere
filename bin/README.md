# 🛠️ 工具脚本目录

本目录包含项目的各种实用脚本。

## 📜 脚本列表

### start.sh / start.bat
**一键启动脚本**

```bash
./bin/start.sh          # macOS/Linux
./bin/start.bat         # Windows
```

用途：
- 自动检查依赖
- 启动 Tauri 开发服务器
- 显示友好的提示信息

### setup-cargo-mirror.sh
**Cargo 镜像源/代理配置工具**

```bash
./bin/setup-cargo-mirror.sh
```

功能：
- 交互式配置 Rust 镜像源
- 支持国内镜像（字节跳动、中科大、清华）
- 支持 VPN 代理配置
- 自动备份现有配置

### diagnose.sh
**环境诊断工具**

```bash
./bin/diagnose.sh
```

检查项目：
- Node.js 和 pnpm 是否安装
- Rust 和 Cargo 是否安装
- Tauri CLI 是否可用
- 项目依赖是否完整
- 配置文件是否存在

### fix-cargo-lock.sh
**Cargo 文件锁修复工具**

```bash
./bin/fix-cargo-lock.sh
```

解决问题：
- "Blocking waiting for file lock" 错误
- Cargo 进程冲突
- 清理构建缓存

## 💡 使用建议

### 首次运行
```bash
./bin/setup-cargo-mirror.sh   # 配置镜像源加速
./bin/start.sh                # 启动应用
```

### 日常开发
```bash
./bin/start.sh                # 每天只需运行这个
```

### 遇到问题
```bash
./bin/diagnose.sh             # 诊断问题
./bin/fix-cargo-lock.sh       # 修复编译问题
```

## 📚 相关文档

- [快速开始](../docs/快速开始.md)
- [快速参考](../docs/快速参考.md)
- [文档索引](../docs/文档索引.md)

## ⚠️ 注意事项

1. 所有脚本需要在项目根目录运行
2. 首次运行前确保脚本有执行权限：
   ```bash
   chmod +x bin/*.sh
   ```
3. Windows 用户使用 `.bat` 脚本
4. macOS/Linux 用户使用 `.sh` 脚本

